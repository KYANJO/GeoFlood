# --------------------------------------------
# List of available cudaflood examples
# --------------------------------------------

if(TARGET GEOFLOOD::GEOCLAW)

    include(cudaflood.cmake)

endif()
